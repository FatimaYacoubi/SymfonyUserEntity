{% block body %}
<div align="center">
<form method="post" action="{{ path('Clientdelete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to Block this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn-primary">Supprimer le Compte</button>
</form> </div>
{% endblock %}