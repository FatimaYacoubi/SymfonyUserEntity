{% extends ('baseback/base.html.twig') %}

{% block body %}

<!DOCTYPE html>

<title>Edit Role</title>
<div align="center">
    <h1>Edit Role</h1>

    {{ include('role/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('role_index') }}">back to list</a>

    {{ include('role/_delete_form.html.twig') }}
    </div>
{% endblock %}
